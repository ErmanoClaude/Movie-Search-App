<h1>This is the reults page</h1>

<ul>
    <% data["Search"].forEach((movie)=> { %>
        <li>
            <strong>
                <%= movie['Title'] %>
            </strong>
             - <%= movie['Year'] %>
        </li>
    <% }); %>
</ul>


<a href="/">Search Again</a>
